# Nunca versionar credenciais
.env
*.key
secrets*

# Dados coletados (podem ser grandes; versione só o que for artefato final)
scripts/dados/
dados/

# Node — só o gerador do deck usa, e é dependência de build
node_modules/
package-lock.json

# Python
__pycache__/
*.pyc
venv/
.venv/

